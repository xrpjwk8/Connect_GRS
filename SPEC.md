# Connect — 대학가 단체 예약 매칭 플랫폼 기획 명세서

> iOS 네이티브 앱 (SwiftUI) · Firebase 기반 · 점주 ↔ 예약자 매칭 서비스

---

## 1. 서비스 개요

**Connect**는 대학가 주변 식당·술집·공간 등의 점주와, 대학 단체(학과·동아리·학생회 등) 예약자를 실시간으로 매칭해주는 단체 예약 플랫폼입니다.

- **핵심 문제**: 대규모 단체 예약은 전화·카카오톡으로 처리되어 점주·예약자 양측 모두 비효율적
- **핵심 해결**: 앱 내 요청서 발송 → 점주 수락 → 즉시 확정의 단방향 매칭 플로우
- **신뢰 장치**: 점주 사업자등록증 인증 + 예약자 대학 이메일(.ac.kr) 인증 + 디포짓(예약금) 결제

---

## 2. 사용자 유형 (역할)

### 점주 (Owner)
| 항목 | 내용 |
|------|------|
| 프로필 노출명 | `[가게명] 사장 [이름]` (예: 땅땅치킨 사장 오효준) |
| 인증 방식 | 사업자등록증 사진 업로드 → 관리자 수동 승인 |
| 신뢰 지표 | 예약 성공률, 노쇼 대응 지수 → 왕관 마크 / VIP 테두리 부여 |

### 예약자 (Booker)
| 항목 | 내용 |
|------|------|
| 프로필 노출명 | `[학교명] [학과/단체] [직책] [이름]` (예: 연세대 인공지능학과 회장 윤태웅) |
| 인증 방식 | 학교 웹메일(.ac.kr) 인증 또는 에브리타임 캡처본 업로드 |
| 신뢰 지표 | 노쇼 프리(No-Show Free) 인증 마크, 거래 횟수별 엠블럼 |

---

## 3. 앱 진입 흐름

```
앱 실행
  └─ 역할 선택 화면 (RoleSelectionView)
       ├─ 점주로 시작하기 → 점주 회원가입
       └─ 예약자로 시작하기 → 예약자 회원가입
```

권한 팝업(카메라, 알림 등)은 최초 실행이 아닌 **실제 필요 시점**에 요청하여 이탈 방지

---

## 4. 화면 목록 및 기능 명세

### 4-1. 공통 화면

#### 역할 선택 (`RoleSelectionView`)
- 점주 / 예약자 카드 선택 UI
- 카드 첫 탭 → 선택 상태 표시, 두 번째 탭 or 하단 버튼 → 가입 화면 이동
- "이미 계정이 있으신가요?" → 로그인 (미구현)

---

### 4-2. 점주 (Owner) 화면

#### 점주 회원가입 (`OwnerSignUpView`)
- 입력 필드
  - 가게명 (최대 20자)
  - 연락처 (숫자만 입력, 키보드 numberPad)
  - 사업자등록번호 (숫자만 입력)
  - 사업자등록증 사진 업로드
- 가입 완료 후 관리자 승인 대기 → 승인 시 서비스 활성화

#### 점주 홈/대시보드 (`OwnerDashboardView`)
- 가게명·점주명 표시
- KPI 카드 2개
  - 이번 주 단체 예약 매출 (전주 대비 증감률)
  - 이번 주 예약 건수 (전주 대비 증감률)
- 대기 중인 예약 요청 목록 (수락 / 거절 버튼)
- 알림 아이콘 (FCM 연동 예정)

#### 예약 관리 캘린더 (`ReservationCalendarView`)
- 월간 캘린더 뷰
- 날짜별 예약 마커(초록점) / 차단 마커(빨간점) 표시
- 날짜 선택 시 해당 일의 타임 블락 관리 화면으로 이동

#### 원터치 타임 블락 (`TimeBlockView`)
- **오프라인 예약 대처 핵심 기능**
- 주간 날짜 탭으로 날짜 선택
- 30분 단위 타임 슬롯 목록
  - `예약 가능` (흰 배경)
  - `🔒 오프라인 예약으로 차단됨` (빨간 배경)
- 슬롯 한 번 터치 → 차단 / 해제 즉시 토글
- 차단된 슬롯은 예약자 검색 결과에서 자동 제외

#### 가게 프로필 관리 (`StoreProfileView`)
- 가게 정보 편집 (가게명, 소개, 카테고리 키워드 태그 최대 15자)
- 사진 업로드

#### 매출 현황 (`SalesView`) — 보류 (TODO)
- PG/오픈뱅킹 연동 없이는 실매출 계산 불가 (사업자 인증 + 외부기관 심사 필요, 절차 복잡)
- 우선순위 밀려 화면 자체를 탭에서 제거함 (ConnectRN 기준, 2026-07-30)
- 추후 PG(토스페이먼츠/포트원 등) 또는 오픈뱅킹 연동 붙인 뒤 재구현

#### 점주 탭 구조 (`OwnerTabView`)
| 탭 | 화면 |
|----|------|
| 홈 | 대시보드 |
| 캘린더 | 예약 관리 캘린더 |
| 프로필 | 가게 프로필 관리 |

---

### 4-3. 예약자 (Booker) 화면

#### 예약자 회원가입 (`BookerSignUpView`)
- 입력 필드
  - 학교명 (드롭다운 선택)
  - 학과/단체명 (직접 입력)
  - 직책 (회장/부회장/총무 등, 선택)
  - 실명
  - 학교 이메일 (.ac.kr 형식 유효성 검사)
  - 인증번호 (6자리, 이메일 발송 후 입력)
- 유효성 검사
  - 이메일 형식 검사 (`@`, `.` 포함) → 인증 요청 버튼 활성화
  - 인증번호 6자리 입력 완료 시 가입 완료 버튼 활성화

#### 예약자 홈 (`BookerHomeView`)
- 위치 라벨: `서울 · {가입 시 선택한 학교명}`
- 검색바 터치 → 필터 모달 시트 오픈
- 카테고리 필터 칩 (전체 / 단체석 / 회식 / 엠티 등)
- 추천 가게 카드 목록 (이미지, 가게명, 지역, 카테고리 태그, 평점, 최소 인원)
- 즐겨찾기 빠른 접근

#### 검색 필터 (`SearchFilterView`)
- 지역 선택 (4열 그리드, 신촌 / 홍대 / 건대 / 혜화 등)
- 날짜 선택 (DatePicker)
- 인원 수 입력
- 시간대 선택 (상관없음 / 오후 / 저녁 / 심야)
- "검색하기" 버튼 → 검색 결과로 이동

#### 검색 결과 (`SearchResultsView`)
- 필터 조건에 맞는 가게 목록
- 점주가 타임 블락한 시간대는 자동 제외
- 가게 카드 탭 → 가게 상세 화면

#### 가게 상세 및 예약 요청 (`StoreDetailView`)
- 가게 정보: 이름, 카테고리, 지역, 최대 수용 인원, 소개, 키워드 태그
- 날짜 선택 (검색 날짜 ±1일)
- **30분 단위 타임 슬롯 선택** (최대 연속 6칸, 과거 시간 비활성)
- 예약 정보 입력
  - 인원 수 (직접 입력)
  - 예산 (1인당, 직접 입력)
  - 행사 목적 (최대 15자 태그)
  - 요청 메시지 (최대 100자)
- 즐겨찾기 토글
- 예약 요청서 발송 → 점주에게 FCM 알림 전송
- 유효성 검사 실패 시 Alert 표시

#### 내 예약 (`MyReservationsView`)
- 탭 필터: 진행 중 / 완료 / 취소
- 진행 중 세분화: 대기 중(pending) / 확정(confirmed)
- 완료 세분화: 완료(completed) / 거절(rejected)
- 예약 카드: 가게명, 날짜·시간, 인원, 상태 배지
- 예약 수정 (`ReservationEditView`) / 취소 기능

#### **예약 슬롯 양도 기능 (킬러 기능)**
- 행사 취소 등 비상 상황 발생 시 확정된 예약을 다른 단체에 양도
- 양도 수락한 단체가 예약 권한 인계

#### 즐겨찾기 (`FavoritesView`)
- 즐겨찾기에 추가한 가게 목록

#### 마이페이지 (`MyPageView`)
- 프로필 정보 확인 (학교, 학과, 직책, 이름)
- 프로필 편집 (`ProfileEditView`) → 사진, 직책 수정 가능

#### 예약자 탭 구조 (`BookerTabView`)
| 탭 | 화면 |
|----|------|
| 홈 | 예약자 홈 |
| 내 예약 | 예약 목록 |
| 즐겨찾기 | 즐겨찾기 |
| 마이페이지 | 마이페이지 |

---

## 5. 예약 상태 흐름

```
예약 요청서 발송 (pending)
  ├─ 점주 수락 → confirmed (즉시 타임 슬롯 차감)
  │     ├─ 행사 완료 → completed
  │     └─ 비상 상황 → [양도] 다른 단체로 인계
  └─ 점주 거절 → rejected
  └─ 예약자 취소 → cancelled
```

---

## 6. 핵심 기능 상세

### 원터치 타임 블락
점주가 전화·워크인 등 오프라인으로 예약을 받은 경우, 앱에서 해당 시간대를 **단 한 번의 터치**로 차단하는 기능.
- 차단 즉시 예약자 검색 결과에서 실시간 제외
- 재터치로 차단 해제 가능

### 실시간 FCM 알림
- 예약자 → 요청서 발송 시 점주에게 푸시 알림
- 점주 → 수락/거절 시 예약자에게 푸시 알림

### 디포짓(예약금) 결제
- 노쇼 방지 목적으로 예약 요청 시 소액 예약금 결제
- 예약 취소 시 환불 정책 적용 (상세 정책 미확정)

### 예약 슬롯 양도
- 확정된 예약을 다른 학과/동아리로 권한 인계
- 양도 시 원 예약자의 디포짓 처리 정책 별도 정의 필요

---

## 7. 기술 스택

| 항목 | 내용 |
|------|------|
| 플랫폼 | iOS (SwiftUI) |
| 백엔드/인증 | Firebase (FCM, Firestore, Auth) |
| 상태 관리 | `@Observable` (AppState) |
| 디자인 시스템 | 자체 `AppColors`, `AppTypography`, `AppRadius` |
| 폰트 | Noto Sans KR (헤드라인), Inter (데이터/레이블) |
| 주요 컬러 | Primary: Lime `#526600` / Ink: `#1b1c1c` / Surface: `#faf9f9` |

---

## 8. 구현 현황

| 화면 | 상태 |
|------|------|
| 역할 선택 | ✅ 완료 |
| 점주 회원가입 | ✅ 완료 (숫자 입력 필터 포함) |
| 예약자 회원가입 | ✅ 완료 (이메일 유효성 + 인증번호 검사) |
| 점주 대시보드 | ✅ 완료 |
| 원터치 타임 블락 | ✅ 완료 |
| 예약 관리 캘린더 | ✅ 완료 |
| 매출 현황 | 🔲 보류 (TODO, PG/오픈뱅킹 연동 후 재구현) |
| 가게 프로필 관리 | ✅ 완료 |
| 예약자 홈 | ✅ 완료 |
| 검색 필터 | ✅ 완료 |
| 검색 결과 | ✅ 완료 |
| 가게 상세 + 예약 요청 | ✅ 완료 |
| 내 예약 | ✅ 완료 |
| 즐겨찾기 | ✅ 완료 |
| 마이페이지 | ✅ 완료 |
| 로그인 화면 | 🔲 미구현 |
| 예약금 결제 연동 | 🔲 미구현 |
| 예약 슬롯 양도 UI | 🔲 미구현 |
| 관리자 승인 대시보드 | 🔲 미구현 |
| 실 서버 연동 (Firestore) | 🔲 미구현 (현재 MockData) |
